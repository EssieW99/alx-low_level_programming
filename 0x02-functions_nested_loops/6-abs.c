#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @e: Character being checked
 *
 * Return: The absolute number
 */
int _abs(int e)
{
	if (e < 0)
	{
		int f;

		f = e * -1;

	return (f);
	}
	return (e);
}

