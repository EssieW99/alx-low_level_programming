#include <stdio.h>

/**
 * main - Entry level
 *
 * Return: Always 0
 */
int main(void)
{
	int ch, ab;

	for (ch = 'a'; ch <= 'z'; ch++)

	putchar(ch);
	for (ab = 'A'; ab <= 'Z'; ab++)

	putchar(ab);
	putchar('\n');

	return (0);
}
